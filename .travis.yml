language: python
python: 2.7
cache:
  directories:
    - $HOME/.cache/pip
    - $HOME/.cache/lektor/builds
install: "pip install lektor"
script: "lektor build"
after_script: "git --version"
deploy:
  provider: script
  script: "lektor deploy ghpages-https"
  on:
    branch: lektor
